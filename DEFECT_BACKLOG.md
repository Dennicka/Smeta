# DEFECT_BACKLOG

Дата обновления: 2026-03-18 (UTC)

Статусы:
- `OPEN` — дефект известен, не закрыт.
- `PARTIAL` — есть частичное решение/обходной путь, но acceptance не закрыт.
- `RESOLVED` — исправление внесено и подтверждено в доступной среде.
- `BLOCKED_ENV` — не может быть закрыт без macOS runtime.

| ID | Дефект | Приоритет | Статус | Expected fix |
|---|---|---:|---|---|
| D-001 | Невозможно подтвердить `.app`/`.dmg` packaging и запуск desktop bundle в текущем Linux окружении. | P0 | BLOCKED_ENV | Провести отдельный macOS release pass: build/sign/package/run, приложить артефакты и обновить acceptance. |
| D-002 | Не подтверждены Preview/Print/PDF export через AppKit/PDFKit runtime. | P0 | BLOCKED_ENV | Выполнить ручные E2E на macOS с evidence (видео/скриншоты/файлы PDF + чек-лист PASS/FAIL). |
| D-003 | Не подтверждён clean-machine install path и first-run/restart lifecycle. | P0 | BLOCKED_ENV | Прогон на чистой macOS машине с фиксацией install/run/restart сценариев и журналом результатов. |
| D-004 | Linux test path стабилизирован: `swift test` больше не падает по `no such module 'SQLite3'`, так как macOS-only `SmetaApp` исключён из Linux package graph. | P1 | RESOLVED | В `Package.swift` добавлен platform-aware wiring: `SmetaApp` product/target объявляется только на macOS, в Linux остаются `SmetaCore` + `SmetaAppTests`; evidence: `EVIDENCE/D004_LINUX_SWIFT_TEST_STABILIZATION.md` с exact commands/raw outputs/exit codes. |
| D-005 | Скрипт `Scripts/stage6_core_verification.swift` не запускался прямой командой `swift Scripts/...` (раньше падал по `@main`/scope и зависимостям), из-за чего не было одной честной воспроизводимой команды запуска. | P1 | RESOLVED | `@main` убран из script-файла, entrypoint вынесен в `Scripts/stage6_core_verification/main.swift`, зафиксирован официальный compile-and-run path одной командой `swiftc ... && /tmp/stage6_core_verification`; полное доказательство с raw output/exit code: `EVIDENCE/D005_STAGE6_CORE_VERIFICATION_RUNNER.md`. |
| D-006 | Stage 6 вердикт остаётся «условно готово только для core logic freeze, НЕ финальный desktop release». | P0 | PARTIAL | Закрыть все macOS-only `blocked_env` сценарии и обновить Final Verification Report до безусловной release-ready оценки. |
| D-007 | Silent/non-user-facing error paths, cancel semantics, orphan/half-success PDF semantics, safe-overwrite semantics, hard-recovery semantics и post-commit false-failure semantics исправлены на code-level, но без macOS runtime UX-подтверждения AppKit цепочек. | P2 | PARTIAL | D-007a + D-007c + D-007e + D-007g + D-007g2 + D-007g3 закрыты на code-level (`EVIDENCE/D007A_ERROR_HANDLING.md`, `EVIDENCE/D007C_OFFERT_CANCEL_SEMANTICS.md`, `EVIDENCE/D007E_ATOMIC_PDF_PERSISTENCE.md`, `EVIDENCE/D007G_SAFE_OVERWRITE_SEMANTICS.md`, `EVIDENCE/D007G2_HARD_RECOVERY_SEMANTICS.md`, `EVIDENCE/D007G3_TEMP_LEAK_AND_POST_COMMIT_SEMANTICS.md`): устранены silent/cancel/orphan/destructive-overwrite, best-effort recovery, temp leak до transaction и false-failure после commit; для финального закрытия D-007 нужен macOS UI runtime проход по cancel/error/failure/overwrite/recovery сценариям. |
| D-008 | Document generation relies on hardcoded/demo lines in views instead of real project/estimate/document mapping. | P0 | RESOLVED | Generation contour для Avtal/Kreditfaktura/ÄTA/Påminnelse переведён на repository-backed `DocumentDraftBuilder` path (без view-level demo строк), с честным incomplete/error поведением и отдельным runtime evidence pack `EVIDENCE/D008_GENERATION_CONTOUR.md`. |
| D-009 | Document snapshot is incomplete and does not store full immutable document content. | P0 | RESOLVED | Подтверждён реальный runtime PASS repository-level flow `finalizeDocumentWithSnapshot(...)` (Linux + рабочий `SQLite3` module map): статус `finalized`, присвоение номера, full snapshot persistence, rollback при ошибке builder и legacy parse path. |
| D-010 | PDF/document export flow is not fully wired for Avtal / Faktura / Kreditfaktura / ÄTA / Påminnelse. | P0 | PARTIAL | После D-010a compile-time blocker в `PDFDocumentService.swift` устранён (см. `EVIDENCE/D010_MACOS_RUNTIME_E2E.md`), поэтому текущий remaining gap для D-010 — именно macOS runtime E2E (AppKit save/export + реальные PDF-файлы и размеры по 5 типам); service-level pipeline evidence остаётся в `EVIDENCE/D010_EXPORT_PIPELINE.md`. |
| D-010a | Compile-time syntax blocker in `Sources/SmetaApp/Services/PDFDocumentService.swift` (`unterminated string literal`) prevented verification of PDF export code path. | P0 | RESOLVED | Исправлена строковая сборка текста в `generateBusinessDocumentPDF(...)`; подтверждено `swiftc -typecheck Sources/SmetaApp/Services/PDFDocumentService.swift` с `EXIT_CODE:0` в `EVIDENCE/D010_MACOS_RUNTIME_E2E.md`. |
| D-011 | CSV client import fake update path removed: import now uses explicit create/update/skip/invalid actions with stable key priority (valid email → externalId), and applies real repository update instead of duplicate insert. | P1 | RESOLVED | Подтверждено evidence pack `EVIDENCE/D011_CLIENT_CSV_IMPORT_UPSERT.md`: сценарии create/update/skip/invalid воспроизводимы, суффикс `"(updated)"` в update-flow отсутствует. |
| D-012 | Calculation contains hardcoded magic percentages for transport/equipment/waste/margin/moms instead of settings/rules. | P0 | RESOLVED | Остаток money-impacting literals закрыт: `0.01/0.1/0.2` перенесены в persisted `calculation_rules` как rule-поля, VAT fallback `0.25` убран из draft-calculation path в пользу persisted `tax_profiles`; runtime evidence и literal-classification table в `EVIDENCE/D012_DEFECT_FIX_8B.md`. |
| D-013 | Migration/update path is weak and relies on opportunistic ALTER TABLE logic rather than a predictable migration flow. | P0 | RESOLVED | Доведено до full schema parity: versioned runner (`schema_migrations` + ordered steps `001/002/003`), восстановлен/зафиксирован Stage5/ops schema tail, runtime parity evidence для fresh+legacy путей в `EVIDENCE/D013B_SCHEMA_PARITY.md` (включая object checklist table/index). |
| D-014 | Acceptance/release documents overstate readiness because some PASS statuses are based on code audit or assumption rather than runtime evidence. | P0 | RESOLVED | Выполнен D-014 documentation/evidence audit: убраны optimistic PASS, введена единая классификация `independently confirmed/repository-claimed/unconfirmed/blocked_env`, синхронизированы `ACCEPTANCE_CHECKLIST.md`, `FINAL_VERIFICATION_REPORT.md`, `CURRENT_STATE.md`; evidence и correction table — `EVIDENCE/D014_ACCEPTANCE_AUDIT.md`. |
| D-015 | Archive/release state is not clean and includes build/output noise inconsistent with a clean release bundle. | P1 | RESOLVED | Верификатор `Scripts/verify_clean_release_d015.sh` усилен до recursive denylist-поиска по всей глубине репозитория (включая `Scripts/`, `Sources/`, `Tests/`): ловит build/output/cache/tmp noise в любом вложенном пути; PASS/FAIL + violations + exit code зафиксированы в `EVIDENCE/D015_CLEAN_RELEASE.md`. |
