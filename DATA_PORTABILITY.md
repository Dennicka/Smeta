# DATA PORTABILITY (Stage 5)

- Full project export bundle: human-readable CSV + machine-friendly JSON manifest.
- Full database export формируется через backup + structured export folder.
- В bundle добавляются metadata: export timestamp, app version, schema version.
